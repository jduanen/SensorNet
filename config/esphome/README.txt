This is a staging area for testing out all of the esphome config files prior to deployment.
All YAML files in this directory can be deleted at will.
This directory gets populated by either the getConfigs.sh script (run locally) or the pushConfigs.sh script (run on the HA server).
